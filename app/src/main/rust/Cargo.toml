[workspace]
members = ["tunnellens-tun2proxy"]
exclude = ["third_party/tun2proxy"]
resolver = "3"

[workspace.package]
edition = "2024"
rust-version = "1.88"
license = "MIT"

[profile.release]
codegen-units = 1
lto = "thin"
opt-level = 3
panic = "unwind"
strip = "symbols"
